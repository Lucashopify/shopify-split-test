name = "split-test-edge"
main = "src/worker.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
# Set these via `wrangler secret put` — never commit real values
# APP_ORIGIN = "https://your-app.fly.dev"
# COOKIE_SECRET = "..."

# KV namespace for assignment cache (optional fast path)
# [[kv_namespaces]]
# binding = "ASSIGNMENTS"
# id = "REPLACE_WITH_KV_ID"
